describe "MotionDynamicType modules" do

  it "should have an MotionDynamicType module" do
    should.not.raise(NameError) { MotionDynamicType }
  end

  it "should have an MDT module" do
    should.not.raise(NameError) { MDT }
  end

end